<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 버튼의 배경색 (흰색) -->
    <solid android:color="@android:color/white" />

    <!-- 둥근 모서리 설정 -->
    <corners android:radius="12dp" />

    <!-- 파란색 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="#006EFD" />

</shape>
